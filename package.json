{
  "name": "fryler",
  "private": true,
  "bin": {
    "fryler": "bin/fryler.ts"
  },
  "type": "module",
  "module": "bin/fryler.ts",
  "scripts": {
    "start": "bun run bin/fryler.ts",
    "dev": "bun --watch bin/fryler.ts",
    "test": "bun test",
    "lint": "oxlint .",
    "fmt": "oxfmt --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.33.0",
    "oxlint": "^1.48.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
